specVersion: "v1beta"
serviceId: "schedule.me"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "schedule.me-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
